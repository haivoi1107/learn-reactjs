import React from 'react';
import PropTypes from 'prop-types';
import './style.css';

Loading.propTypes = {

};

function Loading(props) {
    return (
        <div>

        </div>
    );
}

export default Loading;